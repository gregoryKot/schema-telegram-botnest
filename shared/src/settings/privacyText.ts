// Текст доверительного заявления в настройках «Данные и конфиденциальность»
// — единственная копия (правило №3 CLAUDE.md, В10 аудита 2026-08). webapp и
// miniapp формулировали по-разному («Данные не продаются и не передаются.
// Никогда.» против «…рекламным сетям или третьим лицам…») — берём более
// точную формулировку miniapp. Платформенная разница (вход на сайте может
// быть Google, а не только Telegram) остаётся параметром — сама фраза о
// непередаче данных теперь одна.
export function privacyStorageText(accountLabel: string): string {
  return `Дневник, оценки, заметки, практики, результаты тестов — всё привязано к ${accountLabel} и доступно с любого устройства.`;
}

export const PRIVACY_NO_SHARE_TEXT =
  'Данные не продаются и не передаются рекламным сетям или третьим лицам. Никогда.';
