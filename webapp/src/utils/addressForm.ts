// Единственная копия — в shared (правило №3).
export {
  AddressFormContext,
  useAddressForm,
  useSetAddressForm,
  useTr,
  pickForm,
} from '../../../shared/src/utils/addressForm';
export type { AddressForm, AddressFormCtx } from '../../../shared/src/utils/addressForm';
