// Осознанно НЕ покрыто песочничными тестами (см. gate-sandbox.ts и остальные
// файлы в этой директории для покрытых гейтов):
//
// - scripts/check-eslint-ratchet.mjs — сам гейт запускает `npx eslint . --format
//   json` на ВСЁМ дереве проекта (не на переданных файлах, никакого способа
//   указать поддиректорию нет: команда жёстко `eslint .` от ROOT). Чтобы
//   протестировать его как чёрный ящик, пришлось бы либо (а) поднимать в
//   песочнице полноценный проект с tsconfig/eslint.config.mjs/node_modules
//   (типизированный линтер требует projectService — без реального
//   tsconfig-проекта и установленных зависимостей просто падает с parse-
//   ошибками, которые сам гейт не отличает от настоящих находок), либо
//   (б) фейковать `npx eslint` так же, как makeFakeNpx фейкует `npx
//   jest`/`npx vitest` для coverage-гейтов — но тогда тест проверял бы не
//   гейт, а нашу же заглушку: вся логика eslint-ratchet.mjs, которую стоило
//   бы ловить (JSON.parse отчёта, суммирование errorCount/warningCount по
//   `byRule`, сравнение с бейслайном), тестируется отдельно на fixture-JSON
//   было бы избыточно копировать чужой формат отчёта наугад.
//
// - scripts/check-jscpd-ratchet.mjs — аналогично: гейт запускает `npx jscpd
//   src webapp/src schema-miniapp/src shared/src` на реальных деревьях
//   проекта и читает jscpd-report.json из временной директории, которую сам
//   же создаёт и удаляет. Ставить jscpd в песочнице ради теста дорого
//   (секунды на прогон даже на пустом дереве) и то же самое соображение про
//   "тестируем свою заглушку, а не гейт" применимо и здесь.
//
// В обоих случаях риск не в логике сравнения с бейслайном (она — тонкий
// слой поверх готового JSON и структурно совпадает с уже покрытыми
// check-mutation-ratchet.mjs/check-coverage-ratchet.mjs — тот же паттерн
// "распарсить JSON → сравнить числа с бейслайном → exit 1 при регрессе"),
// а в самом внешнем инструменте — его песочницей не заменить.
it.skip('eslint/jscpd гейты требуют реальных внешних бинарей — не сажаются в песочницу, см. комментарий выше', () => {
  // намеренно пусто — тест-заглушка только для того, чтобы файл был валидным
  // jest-сьютом (jest требует хотя бы один test-блок в файле).
});
