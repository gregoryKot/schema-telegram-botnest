-- Счётчик реально отправленных напоминаний. Ротация текста напоминаний строится
-- по этому счётчику, а не по календарному дню — иначе при фиксированном интервале
-- (напр. раз в 5 дней) остаток dayIndex % N совпадает и юзер видит один и тот же
-- текст подряд. Монотонный seq гарантирует, что два соседних напоминания различаются.
ALTER TABLE "User" ADD COLUMN "notifyReminderSeq" INTEGER NOT NULL DEFAULT 0;
