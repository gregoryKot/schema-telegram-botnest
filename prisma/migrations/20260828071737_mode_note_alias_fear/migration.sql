-- Разбор случая («Что это было») даёт человеку назвать режим своим словом —
-- «Стена», «Гонщик». Своё имя показывается везде, где режим упоминается;
-- клиническое остаётся справкой внутри карточки. Второе поле — вопрос «чего
-- боишься, если этот режим перестанет так делать»: он переворачивает
-- отношение с «во мне дефект» на «во мне охрана».
-- Оба поля — свободный текст, шифруются на уровне приложения
-- (MODE_NOTE_SCHEMA, src/bot/notes.service.ts). Пустая строка по умолчанию,
-- как у остальных полей UserModeNote — существующие карточки не ломаются.
-- AlterTable
ALTER TABLE "UserModeNote" ADD COLUMN     "alias" TEXT NOT NULL DEFAULT '',
ADD COLUMN     "fear" TEXT NOT NULL DEFAULT '';
