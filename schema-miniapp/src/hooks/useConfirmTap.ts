// Тонкий ре-экспорт — источник переехал в shared (правило №3 CLAUDE.md,
// образец: webapp/src/needData.ts). Потребитель — PracticesList.tsx.
export { useConfirmTap } from '../../../shared/src/hooks/useConfirmTap';
