// Пауза перед отправкой оценки потребности.
//
// Зачем пауза вообще: по шкале часто «протаскивают» палец, и без склейки
// улетел бы запрос на каждое промежуточное значение.
//
// Почему 180, а не 500: замер 2026-08-22 (три профиля сети) показал, что
// подтверждение сохранения — а с ним серия, празднование и колесо детства —
// появлялось через 710/787/1130мс, и половину этого времени человек ждал не
// сеть, а нас. 180мс склеивают протаскивание не хуже: внутри одного жеста
// тапы идут чаще, между осознанными тапами по разным шкалам — реже.
export const SAVE_DEBOUNCE_MS = 180;
