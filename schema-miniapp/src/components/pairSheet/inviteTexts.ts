// Общие строки механики «пара» (правило №3/№11 CLAUDE.md): PartnerSection.tsx
// (настройки) и PairSheet.tsx — две независимые реализации одной механики,
// раньше их тексты расходились дословно. Единый источник строк не даёт им
// снова разъехаться. Объединение самих компонентов — отдельная задача.
import { useTr } from '../../utils/addressForm';

export function usePairTexts() {
  const tr = useTr();
  return {
    tr,
    inviteText: tr(
      'Приглашай друга или партнёра — увидите индексы дня друг друга, без подробностей. Только число от 0 до 10 — просто чтобы знать, как дела у другого.',
      'Приглашайте друга или партнёра — увидите индексы дня друг друга, без подробностей. Только число от 0 до 10 — просто чтобы знать, как дела у другого.',
    ),
    copyLabel: tr('Скопируй и отправь другу:', 'Скопируйте и отправьте другу:'),
  };
}
