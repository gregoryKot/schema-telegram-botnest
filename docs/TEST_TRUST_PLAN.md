# План укрепления доверия к тестам (2026-08)

Продолжение `TEST_IMPROVEMENT_PLAN.md` (закрыт: 14/14 этапов, 2334 теста
бэкенда, 81.3% строк, 14 CI-гейтов). Тот план решал вопрос «сколько кода
покрыто». Этот решает вопрос **«можно ли верить зелёному CI»** — он
дороже: 2334 честных теста лучше 4000 самоуспокаивающих.

Порядок пунктов — по риску (вред × вероятность), а не по удобству.

## 1. Фейковая Prisma под всеми e2e — врёт, и мы это уже ловили

**Проблема.** 77 из 79 e2e работают на самописной имитации Prisma
(`test/e2e-support/fake-prisma.ts`). За время работы над планом она трижды
расходилась с настоящей — и каждый раз тесты были ЗЕЛЁНЫЕ:

- не понимала `OR` на верхнем уровне → `disconnect` «успешно» удалял 0 строк;
- не применяла `@default` из схемы → invite/join молча отбивался;
- не поддерживала `include` → связь `TherapyRelation → User` была `undefined`.

Три найденных — это те, на которые случайно наступили. Класс бага «тестовый
двойник врёт» опаснее дыры в покрытии: он делает зелёный CI недостоверным.
Единственный тест на реальном Postgres — `read-after-write` (2 кейса).

**Что делаем.** Ключевые e2e гоняются ВТОРЫМ прогоном против реального
Postgres в джобе `migrations` (инфраструктура уже поднята):
- `app-ownership`, `app-ownership-sweep*`, `therapy-ownership*` — они как раз
  про фильтрацию по `userId`, то есть ровно про то место, где фейк врал;
- `PrismaService` подключается к БД джобы, миграции уже применены;
- сначала — только эти спеки; остальные (auth-flows с моками jose и т.п.)
  остаются на фейке, это осознанно.

**Механизм.** Расхождение фейка с реальной БД теперь падает в CI, а не живёт
годами. Плюс: набор, гоняемый на реальной БД, может только расти — фиксируем
списком в workflow.

## 2. Честность 2300 тестов не измерена

**Проблема.** Mutation-замер сделан только для 4 файлов золотых зон. Там до
работы было **71%** — то есть примерно каждая третья «покрытая» строка не
была защищена. Остальные ~2300 тестов на честность не проверялись вообще;
оснований считать, что там лучше, нет. Покрытие 81% — верхняя граница,
реальная защита ниже.

**Что делаем.** Расширяем `mutate` в `stryker.config.json` волнами по 3-4
сервиса, в порядке цены ошибки:
- волна А: `merge.service` (перенос данных между аккаунтами — ошибка = потеря
  данных), `therapy-relations.service` (privacy), `telegram-auth.guard` (вход);
- волна Б: `notification.planner`, `subscription.lifecycle`, `booking.service`;
- волна В: `bot.service`-семейство.

Прогон — в nightly (в PR-CI ему не место). Каждая волна: замер → разбор
выживших → фиксация бейслайна.

## 3. Фронтенд: пользователь видит именно его

**Проблема.** Бэкенд 81.3%, miniapp ~23%, webapp **13.4%**, shared ~9%.
Компонентных тестов — десятки на ~200 компонентов в каждом фронте. `api`
мокается целиком, поэтому дрейф мока от реального клиента ловит только
TypeScript.

**Что делаем.**
- компонентные тесты на пути, где ошибка стоит денег или доверия: оплата,
  запись к терапевту, дневники, экспорт данных, удаление аккаунта;
- контрактный тест мока: проверка, что мокнутые методы `api` существуют в
  реальном клиенте с той же сигнатурой (тест-сверка, как `table-registry`);
- цель волны — webapp 13.4% → 25%, miniapp 23% → 32%.

## 4. Ни один тест не открывает приложение целиком

**Проблема.** Есть юниты, компонентные и HTTP-e2e. Нет теста, который
проходит пользовательский путь через реальный UI (браузер) — «зашёл, отметил
потребность, увидел её завтра».

**Что делаем.** Playwright уже стоит в окружении. Один smoke-путь на webapp:
логин-заглушка → трекер → сохранение → перезагрузка → значение на месте.
Не «покрытие», а страховка от «собралось, но не работает».

---

## Порядок и статус

| # | Пункт | Статус |
|---|---|---|
| 1 | e2e против реального Postgres | **сделано** — шесть ownership-спеков гоняются дважды (фейк + живая БД) |
| 2 | mutation, волна А | **сделано** — см. находку ниже |
| 3 | фронтенд: платёж/запись/дневники + контракт api-мока | **сделано** — webapp 14.4→17.1%, miniapp 27.5→29.2% |
| 4 | Playwright smoke | **сделано** — прод-сборка в Chromium, джоба browser-smoke в nightly |

## Что показала волна А (главное за весь план)

Замер честности тестов на трёх файлах с самой высокой ценой ошибки:

| Файл | было | смысл |
|---|---|---|
| `merge.service` | **41.3%** | слияние аккаунтов: перенос ВСЕХ данных человека |
| `therapy-relations` | 79.2% | privacy: кто из терапевтов чьи данные видит |
| `telegram-auth.guard` | 92.0% | вход |

`merge.service` покрыт по строкам почти полностью — и при этом 66 поломок
из 155 проходили незамеченными. Можно было обнулить `UNIQUE_RULES`
(правила слияния дублей), стереть имя таблицы из реестра переноса или
потерять `userId2` у пар — CI оставался зелёным. Функция `summarize()`
вообще не была покрыта (25 мутантов без единого теста).

Вывод, ради которого план и писался: **число «81% покрытия» ничего не
говорило о защищённости самого дорогого кода проекта**. Мера честности —
только mutation.

## Побочный улов

Тесты п.3 нашли два дефекта, где провал операции выглядел как успех:
экспорт сводки терапевту падал необработанным промисом («нажал — ничего не
произошло»), а дневник благодарности при отказе API закрывал лист, сообщая
об ошибке только вибрацией (на десктопе её нет). Оба исправлены.

## Волна Б и находки вокруг неё

`merge.service`: `ident()` (единственное место, где имя таблицы подставляется
в SQL текстом) и его белые списки были недостижимы для тестов — функция не
экспортировалась. Защиту можно было выпотрошить при зелёном CI. Теперь
экспортирована и покрыта, плюс сверка реестров между собой: таблица из
`UNIQUE_RULES`, забытая в белом списке, роняла бы merge исключением ПОСРЕДИ
переноса данных живого человека.

**Гейт мёртвых файлов** (`scripts/check-dead-files.mjs`) — незапланированный,
но самый показательный улов. В репозитории лежали два `prisma-exception.filter`:
живой в `src/prisma` и старая копия в `src/filters`, на которую не ссылался
никто. Копия выглядела рабочим кодом — и первый же заход «улучшить обработку
ошибок» ушёл в неё, то есть в код, которого для пользователя нет. Гейт требует
каждому файлу потребителя среди НЕ-тестовых файлов: спек в потребители не
годится, иначе мёртвый код с зелёным тестом считается живым. Первый прогон
нашёл ещё два неподключённых компонента мини-аппа.

**Храповик mutation чинили сам**: он сравнивал общий счёт по всем файлам
прогона, поэтому расширение замера новой волной роняло гейт само по себе —
свежий файл приходит с низким счётом и тянет общий вниз. Сигнал «тесты стали
хуже» без единого ослабевшего теста — это шум, от которого гейт перестают
читать. Теперь общий счёт считается по файлам бейслайна, новые перечисляются
отдельно.

## Что показала волна Б

Замер честности тестов платёжного и напоминательного контура — и работа по
выжившим:

| Файл | было | стало |
|---|---|---|
| `subscription.service` | **67.8%** | 84.4% |
| `booking.service` | 72.2% | 87.9% |
| `slot.service` | 80.2% | 93.7% |
| `notification.planner` | 84.8% | 91.5% |

Самое показательное из выживших: мутация `recurring: true → false` в запросе к
платёжной форме проходила незамеченной. То есть ни один тест не проверял, что
мы вообще просим токенизацию карты — а без неё второе списание по подписке
просто не состоится. Рядом жили: арифметика `invId` (плюс менялся на минус),
сверка суммы платежа с суммой списания, идемпотентность повторного вебхука,
адреса возврата с токеном отмены. Всё это «покрытые» строки.

## Покрытие после волны

| | было | стало |
|---|---|---|
| бэкенд, строки | 81.6% | 88.3% |
| бэкенд, ветки | 70.9% | 79.1% |
| `src/api` (HTTP-периметр) | 49.1% | 89.5% |
| сайт (webapp) | 17.1% | 25.3% |
| мини-апп | 29.2% | 35.0% |
| shared | 29.2% | 47.2% |

## Дальше (не сделано)

- волна В mutation: `bot.service`-семейство;
- фронт: секции сайта (`sections/*`) и экраны упражнений мини-аппа;
- покрытие фронтендов дальше: цель 45% miniapp, 32% webapp;
- волна Г mutation: `telegram`-хендлеры.

Все 14 гейтов теперь под тестами. У eslint- и jscpd-храповиков покрыта та
половина, что может сломаться молча (разбор отчёта и сверка с бейслайном) —
через шов `RATCHET_REPORT_FILE`; сам внешний бинарь проверяется только
прогоном в CI, и это записано в тесте прямым текстом.

Каждый пункт — отдельный PR со своим зелёным CI, храповики обновляются в том
же PR, что и рост.
