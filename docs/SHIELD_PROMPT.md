# Промпт «Щит»: покрыть всё, что может упасть у пользователя

Переиспользуемое задание для агента. Запускать раз в квартал, перед крупным
рефакторингом и после любого инцидента, где поломку нашёл человек, а не система.

Задача промпта — не залатать конкретную дыру, а построить **щит**: перечислить
все места, где код исполняется у пользователя, и для каждого назвать того, кто
за ним следит. Место без наблюдателя — дыра, даже если сегодня там всё цело.
Инцидент 2026-08-08 (`docs/archive/INCIDENT_2026-08-08_TELEGRAM_LOGIN.md`)
случился ровно так: двадцать строк в `public/` не видел ни один инструмент.

---

## Текст промпта (копировать целиком)

> Построй щит покрытия для этого репозитория. Не чини найденные баги —
> закрывай **классы** мест, где баг может прожить незамеченным.
>
> **Шаг 1. Перечисли поверхности исполнения — механически, не по памяти.**
> Поверхность = точка, где код работает у пользователя или в проде. Пройди
> командами (`git ls-files`, grep по декораторам/регистраторам), а не глазами:
> HTTP-маршруты и гарды; хендлеры бота и коллбэки кнопок; крон и планировщики;
> вебхуки; экраны и эффекты фронтов; файлы `public/**` и инлайн-`<script>`;
> service worker и манифест; миграции; `Dockerfile`, entrypoint, `deploy/**`,
> скрипты запуска; сами CI-гейты и merge-драйверы; ветки, зависящие от env;
> швы с чужим кодом (SDK мессенджеров, CDN, платёжные виджеты).
> Результат шага — список, а не ощущение. Скажи вслух его длину.
>
> **Шаг 2. Построй матрицу «поверхность × наблюдатель».**
> Наблюдатели: типы, юнит-тест, e2e на фейке, e2e на реальной БД, CI-гейт,
> рантайм-алерт, метрика в отчёте `/stats`, ручной прогон. В клетке — имя файла
> или джобы, а не «покрыто». Наблюдатель засчитывается, **только если он падает
> при поломке**: если не проверял — не пиши. Пустая строка матрицы = дыра.
>
> **Шаг 3. Сгруппируй дыры в классы и закрывай классом.**
> Дыра почти никогда не единственная: `public/max-bridge.js` был экземпляром
> класса «код вне `src/` невидим для coverage, гейтов и тестов». Для каждого
> класса — один механизм в CI (гейт со своим бейслайном причин), а не N
> точечных тестов. Тест на конкретный файл пиши только тогда, когда класс уже
> закрыт гейтом и остаётся сама логика.
>
> **Шаг 4. Отдельно пройди рантайм — то, что тесты не ловят принципиально.**
> Для каждой ветки отказа спроси: «если это случится у всех сразу, я узнаю от
> системы или от человека?». Каждый экран-тупик обязан слать отчёт наверх;
> каждая ветка «сюда попадать не должны» — логировать и считаться; каждая
> сигнализация — иметь троттлинг (шквал DM = замьюченный чат = ноль алертов);
> каждая новая метрика — появиться в `/stats` простым языком, включая пустое
> состояние. Отдельно проверь швы деплоя: зелёный CI не значит «доехало до
> пользователя».
>
> **Шаг 5. Докажи, что щит работает.**
> Для каждого нового механизма покажи ОБА исхода: сломай защищаемое место —
> механизм краснеет; верни как было — зеленеет. Ложно-красный гейт отключат
> через неделю, а гейт без доказательства краснения не защищает вовсе. Для
> гейтов используй песочницу (`src/test-support/gates/gate-sandbox.ts`), для
> тестов на швах — прогон через настоящего производителя предусловия, а не
> через руками выставленный объект.
>
> **Чего не делать.**
> Не латать каждую дыру отдельным тестом, когда виден класс. Не «чинить»
> ростом бейслайна — бейслайн замораживает долг, а не разрешает его. Не писать
> тесты, проверяющие собственные моки. Не заводить алерт без троттлинга и гейт
> без теста. Не расширять охват молча: любое сознательное исключение приезжает
> с причиной в бейслайне, протухшая запись роняет CI.
>
> **Порядок работы — по радиусу поражения, а не по удобству.**
> Сначала то, что ломается сразу у всех и молча (вход, оплата, сохранение
> данных, деплой), потом частичное и шумное, в конце косметика. Если поломка
> видна пользователю мгновенно, а нам — никогда, это верх списка.
>
> **Формат сдачи.**
> Волнами: каждая волна — отдельный PR с зелёным CI, доведённый до мержа.
> В описании: матрица до/после, какие классы закрыты, доказательство краснения
> каждого механизма и **честный список оставшегося непокрытого** с причиной и
> оценкой риска. Список «что осталось» обязателен: щит без дыр не бывает,
> бывает щит с неизвестными дырами.

---

## Как проверять результат (чеклист ревьюера)

- Матрица есть, и в ней названы **файлы**, а не слово «покрыто»?
- Для каждого закрытого класса есть механизм в CI, а не разовая правка?
- Для каждого механизма показано, что он краснеет на регрессе?
- Новые алерты троттлятся, новые метрики видны в `/stats` человеческим языком?
- Список непокрытого написан честно, с риском, а не пуст?

Пустой список непокрытого — повод не радоваться, а перечитать шаг 1.

## Что уже закрыто (обновлять после каждой волны)

| Класс | Механизм |
| --- | --- |
| Код вне `src/`, который исполняется в браузере | `scripts/check-public-scripts.mjs` |
| Мёртвые файлы и вторые копии живого кода | `scripts/check-dead-files.mjs` |
| Дублирование логики | `scripts/check-jscpd-ratchet.mjs` |
| Разъезд парных файлов двух фронтендов | `scripts/check-paired-files.mjs` |
| Раздувание файлов | `scripts/check-file-size-ratchet.mjs` |
| Просадка покрытия (бэк и три фронт-пакета) | `check-coverage-ratchet.mjs`, `check-frontend-coverage-ratchet.mjs` |
| Битый SQL миграций | джоба `migrations` на реальном Postgres |
| Расхождение фейковой Prisma с настоящей | второй прогон ключевых e2e с `E2E_REAL_DB=1` |
| Дубли маршрутов | `scripts/check-route-collisions.mjs` |
| Тихий отказ входа | `src/api/auth-failure.report.ts` + блок «Вход в мессенджере» в `/stats` |
| Успех входа не считался (виден только рост отказов, не провал успехов) | `src/api/auth-success.report.ts` (событие `auth_success`, троттлинг per userId+host) + расширенный блок «Вход в мессенджере» (`auth-health-metrics.service.ts`/`.format.ts`) |
| Экран-тупик без отчёта наверх | `useAuthFailureReport` (`shared/`, оба фронта) |
| Обвал сборки из-за чужой сети | `deploy/install-openssl.sh` |
| Молчаливая деградация merge-драйверов | `setup-merge-drivers.mjs --check` |
| Сломанный гейт, который молча всё пропускает | тесты в `src/test-support/gates/` |
| Тело запроса без class-validator DTO (правило №6) | `scripts/check-body-dto.mjs` |
| Исполняемый код вне `src/` и вне `public/` (`deploy/**`, `scripts/**`) | `scripts/check-unwatched-code.mjs` |
| Сигнализация без троттлинга (шквал DM = замьюченный чат = ноль алертов) | бюджет в `SecurityLogService` + `scripts/check-alert-throttle.mjs` |
| Деньги пришли, запись не создалась, и все молчат | алерт во всех трёх диапазонах InvId + табличный тест по ним в `payment.controller.spec.ts` |
| Деньги не видны в отчёте | блок «Деньги» — `money-metrics.service.ts` / `money-metrics.format.ts` |
| Рассинхрон путей в деплой-артефактах (Dockerfile ↔ `.dockerignore` ↔ `src/app.module.ts` ↔ `amvera.yml`), невидимый ни tsc/jest/eslint | `scripts/check-deploy-paths.mjs` |
| Образ не собирается (синтаксис Dockerfile, версии, сеть до реестра) | джоба `docker` в `ci.yml` — настоящий `docker build` |
| Прод после деплоя никто не открывал | `.github/workflows/prod-smoke.yml` — `/health`, `/`, бандл мини-аппа на живом сайте |
| Висячий таймер планировщика переживает закрытие пула | `telegram.catchup-timer.ts` + `OnModuleDestroy`, `test/scheduler-teardown.e2e-spec.ts` |
| Ветка, зависящая от конфигурации, деградирует МОЛЧА (не хватает одной env-переменной — фича просто не работает, никто не узнаёт) | `src/infra/capability-report.ts` (реестр) + `capability-boot-log.ts` (ERROR при старте, если сигнализация мертва) + блок «Настройки» в `/stats` + `scripts/check-capability-registry.mjs` |
| Клиентский свидетель есть, пишет, и на него никто не смотрит (`POST /api/client-errors` только логировался — разные поломки схлопывались в один DM за счёт постоянного первого аргумента у `AlertLogger`, а в `/stats` поломки клиента не было вовсе) | событие `client_error` (`userId = null`, троттлинг по source+section+ip — `src/api/client-errors.controller.ts`, `src/api/client-error-section.ts`) + блок «Поломки на клиенте» (`client-error-metrics.service.ts`/`.format.ts`), отдельной строкой выделяет поломки входа |
| Пользователь с формой «вы» видит «ты» (и наоборот) в тексте, живущем вне `tr()`/`t()`/`pickForm()` — императивы без местоимения рядом, зеркальные «вы»-формы, письма бэкенда | `scripts/check-second-person.mjs` |
| Фолбэк-контент (`src/bot/healthy-adult.data.ts`) правится, а сопровождающая миграция для живого пула в БД — нет: читатели канала получают старую версию | `scripts/check-pool-sync.mjs` |

## Известные дыры (обновлять честно)

- **Клиент, который совсем не доехал до сервера (не может отправить ни один
  запрос), по-прежнему невидим с обеих сторон.** И `auth_success`, и
  `auth_rejected` пишет один и тот же guard (`TelegramAuthGuard`) — оба
  события существуют, только если запрос вообще дошёл до сервера. Если DNS не
  резолвится, сети нет, или вебвью падает настолько рано, что не может
  выполнить вообще ни один fetch (включая `POST /api/client-errors`), — не
  пишется НИ отказ, НИ успех, НИ `client_error`. С сервера такая авария
  выглядит не всплеском отказов и не явным провалом успехов, а тихим
  снижением всех счётчиков разом — отличить его от «сегодня было меньше
  желающих открыть приложение» по одним серверным цифрам нельзя, нужна
  внешняя точка отсчёта. Единственный свидетель этого остатка класса —
  прод-смок (`prod-smoke.yml`, видит сайт снаружи, независимо от того, может
  ли вебвью пользователя сходить в сеть).
  Сузилось волной 9 щита: клиент, который ХОТЯ БЫ МОЖЕТ отправить один POST
  (бандл выполнился, JS жив, но авторизация/рендер сломаны — ровно случай
  инцидента 2026-08-08, «аккуратно обработанный экран-тупик»), теперь и
  считается, и виден — `useAuthFailureReport` пишет через
  `POST /api/client-errors`, который стал событием `client_error`
  (`userId = null`, троттлинг по source+section+ip) и отдельной строкой в
  блоке «Поломки на клиенте» (`/stats`), причём поломки входа выделены явно.
  Раньше этот отчёт долетал только до логов хостинга и DM, троттлящегося по
  ПОСТОЯННОМУ первому аргументу — то есть визуально «какая-то поломка была»,
  без числа и без тренда. Теперь число и тренд есть.
- **Живого клиента мессенджера в CI нет.** Между зелёным CI и человеком стоят
  сборка, деплой и вебвью площадки; сегодня их закрывает только наблюдение за
  прод-сайтом после мержа.
- **Долг размеров файлов** заморожен в бейслайне и обязан таять — актуальное
  число файлов сверх потолка 300 показывает `node scripts/check-file-size-ratchet.mjs`
  (хардкодить его здесь бессмысленно: цифра протухает при каждом изменении долга).
- **Тест может обосновывать защиту, которой нет.** В `payment.controller.spec.ts`
  жил зелёный тест с прямым утверждением «алерта тут не надо, алертит сам
  DonationService». На деле в `DonationService` нет ни одного `try/catch`:
  он шлёт DM на успехе и на расхождении суммы, а когда падает сам запрос в
  БД — исключение просто улетает наверх, и не алертит никто. Зелёный тест
  не просто пропустил дыру, он её обосновывал. Гейт такое не поймает: он не
  отличит уверенно сформулированное ложное утверждение от истинного. Что
  ловит — **мутационный прогон**: убери алерт, и набор тестов обязан
  покраснеть. Поэтому денежные и сигнальные пути внесены в золотую зону
  (`payment.controller.ts`, `security-log.service.ts`, `donation.service.ts`
  в `stryker.config.json`). Это сузило класс, но не закрыло: зона покрывает
  15 файлов из сотен, а мутанты гоняются ночью и PR не блокируют. Вне зоны
  защита по-прежнему держится на чтении, что тест реально проверяет.
- **Бюджет алертов режет и настоящий поток, а не только шум.** Закрыв шквал
  (K=3 на событие за 15 минут в `SecurityLogService`), мы приняли размен:
  четвёртый за окно `merge_confirmed` от четвёртого человека доедет числом
  «ещё N», а не отдельным DM. Для редких событий это запас с большим
  зазором, но при массовом событии поштучная видимость теряется — счётчик
  есть, подробностей нет. Правильное лечение — не поднимать K, а завести
  событию свою метрику в `/stats`.
- **Смок прода стоит на grep, а не на коде ответа.** Сервер отдаёт
  SPA-фолбэк: любой неизвестный путь возвращает 200 с index.html сайта
  (проверено, `/health-nope` → 200 + HTML). Значит `curl --fail` поймает
  только полный отказ хоста, а всю различающую работу делают маркеры в теле.
  Выкинут grep «ради простоты» — смок станет зелёным на сломанном
  приложении. Против такого «упрощения» механизма нет, только комментарий
  в самом воркфлоу.
- **У смока прода нет своего троттлинга.** Канал оповещения — письма GitHub
  о упавшем прогоне, а он бежит каждые 15 минут: суточный даунтайм даст
  под сотню писем, то есть тот же замьюченный канал, что и в уроке
  2026-07-29, только медленнее. Осознанный размен: реже — значит дольше не
  знать. Если письма начнут тонуть — резать каденс, а не выключать смок.
- **Реестр возможностей видит только то, что в нём заведено.** `capability-report.ts`
  перечисляет ИЗВЕСТНЫЕ возможности; гейт `check-capability-registry.mjs` ловит
  только конкретную синтаксическую форму — `if (!token/key/secret/process.env.X)
  return …` без `throw`, найденную грепом по ключевым словам в самом условии.
  Новая фича, которая тихо деградирует по-другому (не через `return` сразу за
  `if`, а через несколько строк ниже, через `??`/`||` фолбэк без ветвления,
  через переменную без «говорящего» имени вроде `token`/`key`/`secret`, или
  через чтение `ConfigService.get()` без сравнения с `undefined`), не попадёт
  ни в реестр, ни в бейслайн — гейт её просто не увидит, и класс закрыт лишь
  частично. Расширять список распознаваемых форм придётся по мере находок,
  как и с `check-silent-catch.mjs`.
- **«Файл упомянут в тесте» — всё ещё не «файл исполняется тестом».**
  Комментарии из текста тестов теперь вырезаются (`stripComments` в обоих
  гейтах), и прозой в `//` покрытие больше не подделать — этим и вскрылся
  `eslint.config.mjs`, числившийся покрытым из-за одной строки комментария в
  `cleanup-fixtures.ts`. Но упоминание имени в живом коде теста по-прежнему
  засчитывается, даже если файл не исполняется: статически «этот тест правда
  запускает этот файл» не доказать. Остаток класса держится на ревью.
