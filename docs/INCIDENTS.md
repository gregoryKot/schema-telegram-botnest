# Реестр инцидентов

Одна таблица на все поломки, которые дошли до пользователя или владельца.
Заполняется в том же PR, что и починка (правило №20 CLAUDE.md). Колонка
«кто заметил» — главная метрика щита: пока там стоит «человек», система
слепа к этому классу. Цель — чтобы новые строки появлялись со значением
«система» (DM от самопроверки, алерт трекера, красный смок).

Колонки:
- **Симптом** — что видел человек, а не что было в коде.
- **Корень** — одна фраза, почему.
- **Класс** — к какому повторяющемуся классу относится (см. список ниже).
- **Кто заметил** — пользователь / владелец / система (какой механизм).
- **Сколько жило** — от появления на проде до починки.
- **Починка** — PR.
- **Что держит** — регрессионный тест и/или гейт, который не даст повториться.

## Классы (по итогам разбора 2026-09-16)

1. **Фикстура выдумана** — парсер внешнего формата проверен на том, что
   сочинил автор, а не на том, что присылает площадка. Держит:
   `scripts/check-recorded-fixtures.mjs`.
2. **Конфиг прода** — переменная отсутствует, пуста или указывает не туда,
   код молча деградирует. Держит: `src/infra/env-registry*` +
   `scripts/check-env-registry.mjs`, реестр возможностей.
3. **Инстанс один** — состояние в памяти процесса при втором поде расходится.
   Держит: `CronLeaderService`, Postgres-троттлер; класс закрыт не целиком
   (см. docs/SHIELD_PROMPT.md, «Известные дыры»).
4. **Мок ≠ реальность** — фейковая Prisma или мок драйвера отвечает то, чего
   живая база не умеет. Держит: `scripts/check-raw-sql-live.mjs`, джоба
   `migrations`.
5. **Деплой без проверки** — CI зелёный, деплой прошёл, интеграция мертва,
   узнали через дни. Держит: самопроверка после запуска
   (`src/infra/self-check/`), `prod-smoke.yml`.
6. **Наблюдатель без наблюдателя** — nightly/смок красный, а сигнал никуда не
   уходит. Держит: джоба `alarm` в `nightly.yml` и `prod-smoke.yml`.
7. **Тест не на шве** — предусловие в проде создаёт другой код, тест ставит
   его руками. Держит: seam-тесты (`hostDetection.seam.test.ts` как образец).
8. **Решение по двум сторонам без базы** — инструмент сводит две версии, не
   спросив общего предка, и путает «вторая сторона это место не трогала» с
   «стороны спорят». Держит: трёхстороннее слияние в `merge-json-ratchet.mjs`
   плюс сценарий инцидента в `merge-drivers.spec.ts`.
9. **Проверка в одной среде** — сьюта гоняется в единственной конфигурации
   (раннер всегда `TZ=UTC`), и код, зависящий от среды, для CI не существует.
   Держит: второй прогон под `TZ=Australia/Sydney` — у `jest` (джоба
   `backend`) и у vitest всех трёх пакетов (`shared`, `webapp`, `miniapp`),
   плюс прогон по зонам внутри теста (`forEachTimeZone`).

## Таблица

| Дата | Симптом | Корень | Класс | Кто заметил | Сколько жило | Починка | Что держит |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-16 | Прод в крашлупе несколько часов | Сид в миграции не заполнял `updatedAt NOT NULL`, P3009 заблокировал миграции | мок ≠ реальность | владелец | часы | — | джоба `migrations` (deploy на пустой БД дважды) |
| 2026-07-20 | Старт падал, пока Postgres не готов | Миграции стартовали до готовности БД | деплой без проверки | владелец | — | — | `deploy/wait-for-db.mjs` |
| 2026-07-29 | Чат с алертами замьючен | Сотня DM подряд при одной аварии | наблюдатель без наблюдателя | владелец | — | — | троттлинг алертов, `check-alert-throttle.mjs` |
| 2026-08-08 | Вход в мини-апп не работал ни у одного пользователя Telegram | `max-bridge.js` искал подстроку `WebAppData=` и ловил `tgWebAppData=` | тест не на шве | пользователи | 5 суток | см. `docs/archive/INCIDENT_2026-08-08_TELEGRAM_LOGIN.md` | `check-public-scripts.mjs`, `hostDetection.seam.test.ts`, событие `auth_rejected` |
| 2026-08-26 | Мини-апп из ярлыка тормозил первую минуту | `backdrop-filter: blur` в standalone PWA считался программно | — | владелец | ~неделя | #439 | `check-render-poison.mjs` |
| 2026-08-29 | Живую сессию выбрасывало после ротации refresh | Гонка двух живых токенов трактовалась как кража | мок ≠ реальность | владелец | — | #454, #463, #464 | `test/refresh-rotation.e2e-spec.ts` |
| 2026-08-30 | Уведомления уходили в никуда; `/start` воскрешал слитый аккаунт | Отправка по старому `userId` после слияния | — | владелец | — | #456 | спеки слияния |
| 2026-08-30 | После слияния пропадали «мои схемы», «мои режимы», настройки | Таблицы не были в `USER_OWNED_TABLES` | денормализация без сверки | владелец | — | #455, #477 | гейт реестров моделей |
| 2026-08-31 | Авария БД — десятки одинаковых алертов | Каждый запрос слал свой DM | наблюдатель без наблюдателя | владелец | — | #465 | трекер `dbOutage` (алерт по смене состояния) |
| 2026-09-08 | Вход через Google с домена-алиаса падал «state mismatch» | Кука ставилась на алиасе, колбэк приходил на канонический хост | конфиг прода | система (DM об ошибке фронта) + владелец | — | #484 | `oauth-host.spec.ts`, `redirectToCallbackHost` |
| 2026-09-11 | Три теста restore-репетиции падали с exit 127 всю серию PR | `xxd` нет в минимальном образе; падение списывали на «среду» | наблюдатель без наблюдателя | владелец | недели | #487 | `nightly.yml` → `backup-restore` |
| 2026-09-13 | Запись на консультацию: 500 на каждую попытку, семь попыток подряд | `pg_advisory_xact_lock` через `$queryRaw` — драйвер не читает `void` | мок ≠ реальность | пользователь | — | #490 | `test/booking-slot-lock.e2e-spec.ts`, `check-raw-sql-live.mjs` |
| 2026-09-13 | Слоты шли поверх личных встреч, iCloud отвечал 403 | Корень calendar-home-set попадал в список календарей | фикстура выдумана | владелец, по логам | — | #491 | спек с корнем первым, `caldav-health.ts` |
| 2026-09-13 | Лимит «6 в час» не сработал на семь запросов | Счётчик throttler в памяти одного инстанса | инстанс один | тот же инцидент | — | #492 | `test/throttle-storage.e2e-spec.ts` |
| 2026-09-13 → 16 | Ни один календарь не находился, брони не попадали в Apple Calendar | Регресс #491: регэксп не матчил тег с атрибутами, как у настоящего iCloud | фикстура выдумана | владелец | 3 дня | #494 | `caldav-resourcetype.spec.ts` (9 форм с контролем), `check-recorded-fixtures.mjs` |
| 2026-09-15 | Писем о записях нет | Почта была запасным каналом после Telegram; пустой `ADMIN_EMAIL` молчал | конфиг прода | владелец | ~6 дней | #493 | `notifyAdminBoth`, warn на пустой `ADMIN_EMAIL`, `check-env-registry.mjs` |
| 2026-09-16 | Вход через Google и VK: ERR_TOO_MANY_REDIRECTS | `GOOGLE_REDIRECT_URI` указывал на домен, который мидлвар редиректит обратно | конфиг прода | пользователь | — | #495 | `oauth-redirect-config.ts` (проверка на старте), `oauth-loop-guard.ts` |
| 2026-09-15 → 16 | Два мержа (#494, #495) сутки не доезжали до пользователей: хостинг не пересобрал образ | Amvera не пересобрала после мержа; `/health` и смок отвечали «жив», не «свеж» | деплой без проверки | владелец | ~сутки | #499 | `builtAt` в `/health` + `scripts/check-deploy-lag.mjs` в `prod-smoke.yml` |
| 2026-08-04 → 09-16 | После `git merge origin/main` гейт размеров краснел на файлы, которых ветка не трогала | merge-драйвер брал `min` двух сторон, не глядя на базу, и откатывал рост, принятый в `main` через `--update` | решение по двум сторонам без базы | система (красный `check-file-size-ratchet.mjs` на слитом дереве), но починили тогда руками — `--update` вместо механизма | 6 недель | #502 | пер-ключ трёхстороннее слияние + сценарий «main поднял, ветка не трогала» в `merge-drivers.spec.ts` |
| 2026-09-10 → 16 | Nightly красный 6 из 7 прогонов, никто не знал | `browser-smoke` не поднимал webServer, `detectOpenHandles` висел, mutation не укладывался в 90 минут | наблюдатель без наблюдателя | аудит | 6 дней | аудит 2026-09 | джоба `alarm` (issue + DM) |
| 2026-09-16 | `/health`/`prod-smoke` красные почти круглосуточно (`selfCheck.failed: ["cronLeases"]`) | Порог свежести аренды брался из категории окна `LEASE_WINDOW`, а не из расписания крона — оконный крон (`healthyAdultMorning`/`healthyAdultEvening`) законно молчит ~22 часа между окнами публикации | тест не на шве | система (prod-smoke + ci-alarm #501) | часы | #503 | `cron-gap.ts` (порог из реального расписания) + сверка `CRON_LEASE_REGISTRY` с исходниками через общий сканер + регрессионный тест в `probe-cron-leases.spec.ts` |
| 2026-09-17 | Лента «Моего пути» за неделю теряла запись; `npm test --prefix shared` красный на машине владельца и зелёный под `TZ=UTC` | Календарный день `YYYY-MM-DD` разбирался как `T00:00:00` без `Z` — полночью зоны машины | проверка в одной среде | владелец | — | #513 | `utils/calendarDate.ts` (единственное место разбора), прогон по зонам внутри тестов (`forEachTimeZone`), второй прогон vitest под `TZ=Australia/Sydney` в джобах `shared`/`webapp`/`miniapp` |
| 2026-09-20 | На восточных смещениях календарь истории трекера на сайте съезжал на сутки: после дозаполнения прошлого дня локальное «вчера» выпадало, а первым прошедшим днём вставало сегодня | Живая копия `fillHistoryGaps` в `webapp/HistorySheet.tsx` вела курсор по UTC (`toISOString`), а `todayDate` приходил локальным; в общей версии ту же строку починил #513, до копии фикс не доехал | проверка в одной среде | владелец | со дня копии; после #513 — три дня врозь с общей версией | #540 | копии нет: `HistorySheet` зовёт общую `fillHistoryGaps(h, todayDate)`, день приходит аргументом; регресс в `HistorySheet.backfill.test.tsx` стоит на зафиксированном моменте расхождения зон и краснеет в любой час прогона |
| 2026-09-20 | Запись дневника, сделанную минуту назад, экран «Сегодня» подписывал вчерашней датой, а время показывал по Гринвичу (запись в 22:30 подписана «19:30») | Обратная сторона класса 2026-09-17: МОМЕНТ резали строкой (`createdAt.slice(0, 10)` / `.slice(11, 16)`) и сравнивали гринвичский день с локальным `todayStr()` — стороны сравнения в разных системах координат | проверка в одной среде | владелец | неизвестно (класс жил с появления экрана) | #542 | `shared/src/utils/momentDate.ts` (единственное место, где день и время момента берутся в зоне читателя) + тесты с замороженным «сейчас» на моментах, где гринвичский и локальный день заведомо разные, и обходом зон внутри теста — краснеют в любой час прогона, а не только во второй CI-джобе |

## Что видно по таблице

- **Кто заметил: система — ни разу до 2026-09-08**, и там DM пришёл про
  следствие (ошибка фронта), а не про причину. Все остальные строки —
  человек. Это и есть ответ на вопрос «почему постоянно что-то ломается»:
  ломается не чаще, чем в любом живом проекте, но узнаём об этом последними.
- **Три инцидента из последних семи — один класс** (фикстура выдумана /
  конфиг прода), и каждый чинили как строку, а не как класс. Классы
  закрываются механизмами из списка выше.
- **Фикс-PR сам принёс регресс** (#491 → #494), и регресс жил три дня, потому
  что после деплоя интеграцию никто не проверял. Самопроверка после запуска
  закрывает ровно этот зазор.

## Как добавлять строку

Строка добавляется в PR починки, вместе с регрессионным тестом. Если класса
в списке нет — это новый класс, и он заслуживает механизма, а не только
строки: правило №15 (гейт чинят, а не замалчивают) действует и здесь.
