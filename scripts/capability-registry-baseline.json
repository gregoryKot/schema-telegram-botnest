{
  "src/therapy/therapist-request.service.ts": "BOT_TOKEN уже обязателен для старта приложения (src/telegram/telegram.providers.ts бросает при его отсутствии, TelegramModule подключён безусловно в src/app.module.ts) — эта проверка защитная и в реальности недостижима. К тому же при delivered=false notifyAdmin() сам логирует error и падает в notifyAdminWithFallback (e-mail) — деградация уже компенсирована в этом же файле, отдельная запись реестра ничего не добавит."
}
